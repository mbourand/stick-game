// Dev/fallback placeholder. In production this file is overwritten at
// container startup with values from the runtime environment (Dokploy).
// During `vite dev`/local build, values come from import.meta.env instead.
window.__ENV__ = window.__ENV__ || {};
