# Nixpacks auto-detects pnpm from `packageManager` in package.json and
# pnpm-lock.yaml, runs `pnpm install --frozen-lockfile`, then this build cmd.
[phases.build]
cmds = ["pnpm front:build:production"]

[variables]
NIXPACKS_NODE_VERSION = "22"
